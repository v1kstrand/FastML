Complete this form before running the application by filling in the 
details after each '->' sign.  or ' ' are not needed.

Example: ' task_type - type: regression / classification -> classification '

# task_type - type [ regression / classification ] ->regression

# csv_path - Path to csv data, example [ C:/Users/.../data.csv ]->csv\synthetic_regression.csv

# target - Target column name, example [ target ]->target

# scale - type [ zscore / minmax / none ] -> zscore

# split_size - Train test split size (0.05 <= x <= 0.95), example [ 0.3 ]->0.3

# model_name - Final model name, example [ final_model ]->final_model

# drop_columns - (Optional) Columns to drop, separate with ',' example [ col1,col2,col3 ]->